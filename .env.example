# CoordMCP Environment Configuration

# Data storage directory (default: ~/.coordmcp/data)
# COORDMCP_DATA_DIR=/path/to/data

# Log level (DEBUG, INFO, WARNING, ERROR)
# COORDMCP_LOG_LEVEL=INFO

# Maximum file locks per agent
# COORDMCP_MAX_FILE_LOCKS_PER_AGENT=100

# Lock timeout in hours
# COORDMCP_LOCK_TIMEOUT_HOURS=24

# Enable compression (future feature)
# COORDMCP_ENABLE_COMPRESSION=false
